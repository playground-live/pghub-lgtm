source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in pghub-lgtm.gemspec
gemspec

gem "pghub-base", path: "../pghub-base"
